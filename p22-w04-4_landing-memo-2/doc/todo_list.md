# Implementation To-Do List

## Phase 0: Project Foundation Setup
1. [pending] Create Next.js project with App Router, TypeScript, ESLint, Tailwind CSS enabled.
2. [pending] Configure ESLint and Prettier for consistent code formatting.
3. [pending] Set up `app/layout.tsx` with basic HTML structure, `lang="ko"`, and `body` with default background and text colors.
4. [pending] Integrate Pretendard font using `next/font/local` and apply it to `RootLayout`.
5. [pending] Configure `tailwind.config.ts` to extend colors (e.g., primary) and content paths.
6. [pending] Verify project runs locally and Tailwind/font styles are applied correctly.

## Phase 1: Static Hero Section (No Animation)
1. [pending] Implement the Header component with "MemoApp" logo, navigation links ("기능", "사용 사례", "가격", "로그인"), and "무료로 시작하기" CTA button.
2. [pending] Implement the main Hero content area including "NEW VERSION 2.0" as a sub-message, "생각이 떠오르는 순간, 바로 메모" as the main headline, and "회의 중에도, 길을 걷다가, 침대에 누워서도 당신의 모든 영감을 가장 빠르고 간편하게 기록하세요." as the sub-text.
3. [pending] Create Primary ("무료로 시작하기") and Secondary ("앱 다운로드") CTA buttons with basic styling (no hover animations yet).
4. [pending] Implement the static floating memo cards (3-5 cards) with glassmorphism style, but without parallax or hover animations.
5. [pending] Ensure responsive typography is applied to all Hero section text elements.
6. [pending] Verify the static Hero section layout on mobile and desktop breakpoints.